# .env.production (example for Coolify/production)
# --- Database ---
POSTGRES_DB=rest_db # Name of your production database
POSTGRES_USER=strapi # Database username
POSTGRES_PASSWORD=EqgIMfCJ1j # Database password (use a strong, random value)
POSTGRES_HOST=postgres # Do not change unless using external DB
POSTGRES_PORT=5432

# --- Strapi ---
NODE_ENV=production
DATABASE_CLIENT=postgres
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=rest_db # Must match POSTGRES_DB
DATABASE_USERNAME=strapi # Must match POSTGRES_USER
DATABASE_PASSWORD=EqgIMfCJ1j # Must match POSTGRES_PASSWORD
APP_KEYS=QoxeZpJEA3,Gz03NuTj26 # Comma-separated, random strings
API_TOKEN_SALT=your_api_token_salt # Random string
ADMIN_JWT_SECRET=jocvHpYS53 # Random string
JWT_SECRET=KCP3kzDMiU # Random string

# --- Next.js ---
NEXT_PUBLIC_STRAPI_URL=https://ib.yonkerslu.com # Public URL for Strapi API

# Fill all values with strong, unique secrets. Use your password generator for all *_SECRET, *_SALT, and APP_KEYS values.
# For local testing, copy this file to .env and adjust as needed.

